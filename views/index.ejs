<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content.title %></title>
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>">
    <meta property="og:title" content="<%= content.inLovingMemoryOf %> <%= obituary.name %> | സ്നേഹപൂർവ്വം ഓർത്ത്">
    <meta property="og:description" content="Memorial website for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Share your condolences and cherished memories. | <%= obituary.name %> ന്റെ സ്മാരക വെബ്സൈറ്റ്. നിങ്ങളുടെ അനുശോചനങ്ങളും പ്രിയപ്പെട്ട ഓർമ്മകളും പങ്കിടുക.">
    <meta property="og:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>">
    <meta property="twitter:title" content="<%= content.inLovingMemoryOf %> <%= obituary.name %> | സ്നേഹപൂർവ്വം ഓർത്ത്">
    <meta property="twitter:description" content="Memorial website for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Share your condolences and cherished memories. | <%= obituary.name %> ന്റെ സ്മാരക വെബ്സൈറ്റ്. നിങ്ങളുടെ അനുശോചനങ്ങളും പ്രിയപ്പെട്ട ഓർമ്മകളും പങ്കിടുക.">
    <meta property="twitter:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    
    <!-- Additional Meta Tags -->
    <meta name="description" content="Memorial website for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Share your condolences and cherished memories. | <%= obituary.name %> ന്റെ സ്മാരക വെബ്സൈറ്റ്. നിങ്ങളുടെ അനുശോചനങ്ങളും പ്രിയപ്പെട്ട ഓർമ്മകളും പങ്കിടുക.">
    <meta name="keywords" content="memorial, obituary, <%= obituary.name %>, condolences, remembrance, സ്മാരകം, അനുശോചനം, ഓർമ്മ">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= content.title %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link active" href="/?lang=<%= currentLang %>"><%= content.biography %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/funeral?lang=<%= currentLang %>"><%= content.funeral %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/photos?lang=<%= currentLang %>"><%= content.photos %></a></li>
                    <li class="nav-item"><a class="nav-link" href="#condolences"><%= content.condolences %></a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <%= content.language %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="?lang=english">English</a></li>
                            <li><a class="dropdown-item" href="?lang=malayalam">മലയാളം</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main>
        <div class="hero-section">
            <div class="container">
                <div class="row">
                    <div class="col-md-8 mx-auto text-center">
                        <div class="memorial-photo-large">
                            <img src="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg" alt="<%= obituary.name %>">
                            
                            <div class="memorial-card">
                                <div class="memorial-prefix"><%= content.inLovingMemoryOf %></div>
                                <div class="memorial-name-large"><%= obituary.name %></div>
                                <div class="memorial-dates">
                                    <%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - 
                                    <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="decorative-pattern"></div>
        </div>

        <div class="decorative-border"></div>

        <div class="container py-5">
            <div class="row">
                <div class="col-lg-8">
                    <section class="mb-5">
                        <h2><%= content.biography %></h2>
                        <p class="lead"><%= obituary.biography %></p>
                    </section>

                    <% if (obituary.survivedBy && obituary.survivedBy.length > 0) { %>
                    <section class="mb-5">
                        <h3><%= content.survivedBy %></h3>
                        <ul class="list-unstyled">
                            <% obituary.survivedBy.forEach(person => { %>
                                <li class="mb-2"><%= person %></li>
                            <% }) %>
                        </ul>
                    </section>
                    <% } %>

                    <% if (obituary.tribute) { %>
                    <section class="mb-5">
                        <h3><%= content.tribute %></h3>
                        <p><%= obituary.tribute %></p>
                    </section>
                    <% } %>
                </div>

                <div class="col-lg-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= content.funeral %></h5>
                            <p><strong><%= content.venue %>:</strong> <%= obituary.funeral.venue %></p>
                            <p><strong><%= content.dateTime %>:</strong> 
                                <%= new Date(obituary.funeral.date).toLocaleDateString() %> 
                                at <%= obituary.funeral.time %>
                            </p>
                            <a href="/funeral" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </div>
            </div>

            <section id="condolences" class="py-5">
                <h2><%= content.condolences %></h2>
                
                <div class="row">
                    <div class="col-lg-6">
                        <form id="condolenceForm" class="mb-4">
                            <h4><%= content.submitCondolence %></h4>
                            <div class="mb-3">
                                <label for="name" class="form-label"><%= content.name %></label>
                                <input type="text" class="form-control" id="name" required>
                            </div>
                            <div class="mb-3">
                                <label for="email" class="form-label"><%= content.email %></label>
                                <input type="email" class="form-control" id="email" required>
                            </div>
                            <div class="mb-3">
                                <label for="message" class="form-label"><%= content.message %></label>
                                <textarea class="form-control" id="message" rows="4" required></textarea>
                            </div>
                            <button type="submit" class="btn btn-primary"><%= content.submit %></button>
                        </form>
                    </div>
                    
                    <div class="col-lg-6">
                        <h4><%= content.recentMessages %></h4>
                        <div id="condolencesList">
                            <% if (condolences && condolences.length > 0) { %>
                                <% condolences.forEach(condolence => { %>
                                    <div class="card mb-3">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start mb-2">
                                                <h6 class="card-title mb-0"><%= condolence.name %></h6>
                                                <small class="text-muted">
                                                    <%= new Date(condolence.createdAt).toLocaleDateString() %>
                                                </small>
                                            </div>
                                            <p class="card-text"><%= condolence.message %></p>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="text-center py-4">
                                    <p class="text-muted">No condolence messages yet. Be the first to share your thoughts.</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </section>

            <p class="elegant-text">"In loving memory, forever in our hearts"</p>
        </div>

        <div class="decorative-border"></div>
    </main>

    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container text-center">
            <h5 class="memorial-footer-name mb-2"><%= obituary.name %></h5>
            <p class="memorial-footer-dates mb-3">
                <%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - 
                <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>
            </p>
            <p class="mb-0"><%= obituary.name %>'s Family</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
