<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content.photos %> - <%= content.title %></title>
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>/photos">
    <meta property="og:title" content="<%= content.familyPhotoGallery %> - <%= obituary.name %> | കുടുംബ ഫോട്ടോ ഗാലറി">
    <meta property="og:description" content="View cherished family photos and memories of <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). | <%= obituary.name %> ന്റെ പ്രിയപ്പെട്ട കുടുംബ ഫോട്ടോകളും ഓർമ്മകളും കാണുക.">
    <meta property="og:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>/photos">
    <meta property="twitter:title" content="<%= content.familyPhotoGallery %> - <%= obituary.name %> | കുടുംബ ഫോട്ടോ ഗാലറി">
    <meta property="twitter:description" content="View cherished family photos and memories of <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). | <%= obituary.name %> ന്റെ പ്രിയപ്പെട്ട കുടുംബ ഫോട്ടോകളും ഓർമ്മകളും കാണുക.">
    <meta property="twitter:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    
    <!-- Additional Meta Tags -->
    <meta name="description" content="View cherished family photos and memories of <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). | <%= obituary.name %> ന്റെ പ്രിയപ്പെട്ട കുടുംബ ഫോട്ടോകളും ഓർമ്മകളും കാണുക.">
    <meta name="keywords" content="photos, gallery, memorial, <%= obituary.name %>, memories, family photos, ഫോട്ടോകൾ, ഗാലറി, സ്മാരകം, ഓർമ്മകൾ, കുടുംബ ഫോട്ടോകൾ">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= content.title %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/?lang=<%= currentLang %>"><%= content.biography %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/funeral?lang=<%= currentLang %>"><%= content.funeral %></a></li>
                    <li class="nav-item"><a class="nav-link active" href="/photos?lang=<%= currentLang %>"><%= content.photos %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/?lang=<%= currentLang %>#condolences"><%= content.condolences %></a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <%= content.language %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/photos?lang=malayalam">മലയാളം</a></li>
                            <li><a class="dropdown-item" href="/photos?lang=english">English</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main>
        <div class="container py-5">
            <% if (photos && photos.length > 0) { %>
                <!-- Photo count info -->
                <div class="photo-info mb-4 text-center">
                    <h4 class="mb-3"><%= content.familyPhotoGallery %></h4>
                    <p class="text-muted">
                        <span id="currentPhotoNum">1</span> of <%= pagination.totalPhotos %> Photos
                    </p>
                </div>

                <!-- Slideshow Gallery -->
                <div class="slideshow-container mb-5">
                    <% photos.forEach((photo, index) => { %>
                        <div class="slide <%= index === 0 ? 'active' : '' %>" data-slide="<%= index %>">
                            <img src="<%= photo.url %>" alt="<%= photo.caption || 'Family Memory' %>" onclick="openFullscreen('<%= photo.url %>', <%= index %>)">
                            <% if (photo.caption && photo.caption !== `Photo ${photo.position}`) { %>
                            <div class="slide-caption">
                                <p><%= photo.caption %></p>
                            </div>
                            <% } %>
                        </div>
                    <% }) %>
                    
                    <!-- Navigation arrows -->
                    <a class="prev" onclick="changeSlide(-1)">&#10094;</a>
                    <a class="next" onclick="changeSlide(1)">&#10095;</a>
                </div>

            <% } else { %>
                <div class="row">
                    <div class="col-md-8 mx-auto text-center">
                        <div class="empty-state py-5">
                            <h3 class="text-muted mb-4"><%= content.galleryComingSoon %></h3>
                            <p class="lead text-muted">
                                <%= content.memoriesWillBeShared %>
                            </p>
                            <a href="/?lang=<%= currentLang %>" class="btn btn-primary"><%= content.returnToMemorial %></a>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>

        <!-- Fullscreen Modal -->
        <div id="fullscreenModal" class="fullscreen-modal">
            <span class="fullscreen-close" onclick="closeFullscreen()">&times;</span>
            <img id="fullscreenImg" class="fullscreen-content">
            <div class="fullscreen-nav">
                <a class="fullscreen-prev" onclick="fullscreenPrev()">&#10094;</a>
                <a class="fullscreen-next" onclick="fullscreenNext()">&#10095;</a>
            </div>
            <div id="fullscreenCaption" class="fullscreen-caption"></div>
        </div>
    </main>

    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container text-center">
            <h5 class="memorial-footer-name mb-2"><%= obituary.name %></h5>
            <p class="memorial-footer-dates mb-3">
                <%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - 
                <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>
            </p>
            <p class="mb-0"><%= obituary.name %>'s Family</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
