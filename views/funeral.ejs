<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content.funeral %> - <%= content.title %></title>
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>/funeral">
    <meta property="og:title" content="<%= content.funeral %> - <%= obituary.name %> | ശവസംസ്കാര വിവരങ്ങൾ">
    <meta property="og:description" content="Funeral service details for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Join us in celebrating their life and legacy. | <%= obituary.name %> ന്റെ ശവസംസ്കാര വിവരങ്ങൾ. അവരുടെ ജീവിതവും പാരമ്പര്യവും ആഘോഷിക്കാൻ ഞങ്ങളോടൊപ്പം ചേരുക.">
    <meta property="og:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="<%= process.env.WEBSITE_URL || 'http://localhost:3000' %>/funeral">
    <meta property="twitter:title" content="<%= content.funeral %> - <%= obituary.name %> | ശവസംസ്കാര വിവരങ്ങൾ">
    <meta property="twitter:description" content="Funeral service details for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Join us in celebrating their life and legacy. | <%= obituary.name %> ന്റെ ശവസംസ്കാര വിവരങ്ങൾ. അവരുടെ ജീവിതവും പാരമ്പര്യവും ആഘോഷിക്കാൻ ഞങ്ങളോടൊപ്പം ചേരുക.">
    <meta property="twitter:image" content="https://res.cloudinary.com/dl4p1qeva/image/upload/v1754085891/default-avatar-photo-placeholder-profile-picture-vector_wppcnd.jpg">
    
    <!-- Additional Meta Tags -->
    <meta name="description" content="Funeral service details for <%= obituary.name %> (<%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>). Join us in celebrating their life and legacy. | <%= obituary.name %> ന്റെ ശവസംസ്കാര വിവരങ്ങൾ. അവരുടെ ജീവിതവും പാരമ്പര്യവും ആഘോഷിക്കാൻ ഞങ്ങളോടൊപ്പം ചേരുക.">
    <meta name="keywords" content="funeral, service, memorial, <%= obituary.name %>, ceremony, celebration of life, ശവസംസ്കാരം, സേവനം, സ്മാരകം, ചടങ്ങ്, ജീവിത ആഘോഷം">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= content.title %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/?lang=<%= currentLang %>"><%= content.biography %></a></li>
                    <li class="nav-item"><a class="nav-link active" href="/funeral?lang=<%= currentLang %>"><%= content.funeral %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/photos?lang=<%= currentLang %>"><%= content.photos %></a></li>
                    <li class="nav-item"><a class="nav-link" href="/?lang=<%= currentLang %>#condolences"><%= content.condolences %></a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <%= content.language %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/funeral?lang=malayalam">മലയാളം</a></li>
                            <li><a class="dropdown-item" href="/funeral?lang=english">English</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main>
        <div class="hero-section">
            <div class="container">
                <div class="row">
                    <div class="col-md-8 mx-auto text-center">
                        <h1 class="display-4 mb-2"><%= content.funeral %></h1>
                    </div>
                </div>
            </div>
        </div>

        <div class="container py-4">
            <div class="row">
                <div class="col-lg-8">
                    <div class="card mb-4">
                        <div class="card-body">
                            <h3 class="card-title mb-4"><%= content.serviceInfo %></h3>
                            
                            <div class="row mb-3">
                                <div class="col-sm-3"><strong><%= content.venue %>:</strong></div>
                                <div class="col-sm-9"><%= obituary.funeral.venue %></div>
                            </div>
                            
                            <div class="row mb-3">
                                <div class="col-sm-3"><strong><%= content.address %>:</strong></div>
                                <div class="col-sm-9"><%= obituary.funeral.address %></div>
                            </div>
                            
                            <div class="row mb-3">
                                <div class="col-sm-3"><strong>Date:</strong></div>
                                <div class="col-sm-9"><%= new Date(obituary.funeral.date).toLocaleDateString() %></div>
                            </div>
                            
                            <div class="row mb-4">
                                <div class="col-sm-3"><strong>Time:</strong></div>
                                <div class="col-sm-9"><%= obituary.funeral.time %></div>
                            </div>

                            <div class="d-flex flex-wrap gap-2">
                                <% if (obituary.funeral.googleMapsUrl) { %>
                                    <a href="<%= obituary.funeral.googleMapsUrl %>" target="_blank" class="btn btn-primary">
                                        <%= content.directions %>
                                    </a>
                                <% } %>
                                
                                <% if (obituary.funeral.liveStreamUrl) { %>
                                    <a href="<%= obituary.funeral.liveStreamUrl %>" target="_blank" class="btn btn-danger">
                                        <%= content.liveStream %>
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title"><%= content.location %></h4>
                            <p><%= obituary.funeral.address %></p>
                            <% if (obituary.funeral.googleMapsUrl) { %>
                                <div class="map-container">
                                    <iframe 
                                        src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3!2d0!3d0!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2z!5e0!3m2!1sen!2s!4v1234567890"
                                        width="100%" 
                                        height="300" 
                                        style="border:0;" 
                                        allowfullscreen="" 
                                        loading="lazy">
                                    </iframe>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4">
                    <% if (obituary.funeral.liveStreamUrl) { %>
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title"><%= content.liveStream %></h5>
                            <p class="card-text">Join us virtually for the service</p>
                            <div class="live-stream">
                                <iframe 
                                    src="<%= obituary.funeral.liveStreamUrl.replace('watch?v=', 'embed/') %>"
                                    frameborder="0" 
                                    allowfullscreen>
                                </iframe>
                            </div>
                        </div>
                    </div>
                    <% } %>

                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= content.additionalInfo %></h5>
                            <p class="card-text">
                                <%= content.celebrateLife %> <%= obituary.name %> <%= content.honorMemory %>
                            </p>
                            <p class="card-text">
                                <small class="text-muted">
                                    <%= content.contactFamily %>
                                </small>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container text-center">
            <h5 class="memorial-footer-name mb-2"><%= obituary.name %></h5>
            <p class="memorial-footer-dates mb-3">
                <%= new Date(obituary.dateOfBirth).toLocaleDateString() %> - 
                <%= new Date(obituary.dateOfPassing).toLocaleDateString() %>
            </p>
            <p class="mb-0"><%= obituary.name %>'s Family</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>
